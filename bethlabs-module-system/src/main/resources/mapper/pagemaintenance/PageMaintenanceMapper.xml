<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bethlabs.module.system.dal.mysql.pagemaintenance.PageMaintenanceMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="selectPage"
            resultType="com.bethlabs.module.system.controller.admin.pagemaintenance.vo.PageMaintenanceRespVO">
        select
            t1.*,
            t2.product_name
        from ops_page_maintenance t1
        inner join ops_product t2 on t1.product_id = t2.id
        <where>
            <if test="vo.productId != null">
                and t1.product_id = #{vo.productId}
            </if>
            <if test="vo.isEnable != null">
                and t1.is_enable = #{vo.isEnable}
            </if>
            <if test="vo.versionNumber != null and vo.versionNumber != ''">
                and t1.version_number like concat('%', #{vo.versionNumber}, '%')
            </if>
        </where>
        order by t1.id desc
    </select>
</mapper>