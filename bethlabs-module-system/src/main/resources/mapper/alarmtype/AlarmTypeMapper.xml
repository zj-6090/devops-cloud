<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bethlabs.module.system.dal.mysql.alarmtype.AlarmTypeMapper">

    <!--
        一般情况下，尽可能使用 Mapper 进行 CRUD 增删改查即可。
        无法满足的场景，例如说多表关联查询，才使用 XML 编写 SQL。
        代码生成器暂时只生成 Mapper XML 文件本身，更多推荐 MybatisX 快速开发插件来生成查询。
        文档可见：https://www.iocoder.cn/MyBatis/x-plugins/
     -->

    <select id="selectPage"
            resultType="com.bethlabs.module.system.controller.admin.alarmtype.vo.AlarmTypeRespVO">
        select
            t1.id,
            t1.device_alarm_code,
            t1.product_id,
            t1.alarm_level_id,
            t2.alarm_name,
            t2.alarm_type alarm_type_code,
            t2.alarm_level
        from ops_alarm_type t1
        inner join ops_alarmlevel t2 on t1.alarm_level_id = t2.id
        where t1.product_id = #{vo.productId}
        order by t1.id desc
    </select>
</mapper>